
file-protocols:
  - name: Lombok Config
    description: Lombok Configured appropriately
    tags:
      - lombok
      - config
    repository-paths: lombok.config # Extra "s" at the end
    required: true
    severity: WARNING