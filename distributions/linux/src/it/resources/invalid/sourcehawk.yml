

file-protocols:
  - name: File Properties
    description: File Properties configured correctly
    tags:
      - config
    # Missing repository-path
    required: true
    severity: WARNING
    enforcers:
      - STRING-PROPERTY-EQUALS(key, value)
      - STRING-PROPERTY-EQUALS(foo, bar)